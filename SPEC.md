# SPEC

I want to create a MOD for the game 7days2die. It only needs to work for local single player games.

The MOD should a new block to the game. The block should function as an OR gate, it should be able to accept multiple electric inputs and if one of them is `true` then the output should be `true`.

By default the game does not support blocks having multiple inputs.

The block should also have a way to disconnect connections, make some suggestions on how to do this do this.

Use @search-specialist subagents in parallel to research information on modding, we will most likely need to mod with C# and unity. Here are some good places to start but maybe you find more on the web:
- https://7daystodie.fandom.com/wiki/Modding
- https://community.thefunpimps.com/categories/game-modification.48/

Use a @game-developer subagent to use the discovered documentation to create a plan on how to create this MOD.